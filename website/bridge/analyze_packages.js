import getUrlFromPackageJson from parser;

function analyze_package(){
    // get package from database

    // run parser on it

    // pass into repo analyzer
    // call run_url() -> this returns nothing, prints to stdout

    // create schema 
    
    // save into database

}